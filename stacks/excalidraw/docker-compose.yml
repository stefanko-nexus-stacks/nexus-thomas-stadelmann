services:
  excalidraw:
    image: ${IMAGE_EXCALIDRAW:-excalidraw/excalidraw:latest}
    container_name: excalidraw
    restart: unless-stopped
    ports:
      - "8082:80"
    networks:
      - app-network

networks:
  app-network:
    external: true
