services:
  it-tools:
    image: ${IMAGE_IT_TOOLS:-corentinth/it-tools:latest}
    container_name: it-tools
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - app-network

networks:
  app-network:
    external: true
